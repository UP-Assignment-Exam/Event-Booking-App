import 'package:get/get.dart';
// import your controllers/services

class InitialBinding extends Bindings {
  @override
  void dependencies() {
    // Get.put(YourController());
    // Get.lazyPut(() => YourService());
  }
}
// import your controllers/services

class signin_screen extends Bindings {
  @override
  void dependencies() {
    // Get.put(YourController());
    // Get.lazyPut(() => YourService());
  }
}

// import your controllers/services

class signup_screen extends Bindings {
  @override
  void dependencies() {
    // Get.put(YourController());
    // Get.lazyPut(() => YourService());
  }
}

class reset_password_screen extends Bindings {
  @override
  void dependencies() {
    // Get.put(YourController());
    // Get.lazyPut(() => YourService());
  }
}
class verification_screen extends Bindings {
  @override
  void dependencies() {
    // Get.put(YourController());
    // Get.lazyPut(() => YourService());
  }
}
