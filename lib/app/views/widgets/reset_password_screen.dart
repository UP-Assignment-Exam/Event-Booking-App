// import 'package:event_booking_app/app/auth/reset_password_screen.dart';
import 'package:flutter/material.dart';

class reset_password_screen extends StatelessWidget {
  const reset_password_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('reset_password_screen'),
      ),
      body: const Center(
        child: Text('reset_password_screen the form!'),
      ),
    );
  }
}
