// import 'package:event_booking_app/app/auth/signup_screen.dart';
import 'package:flutter/material.dart';

class signup_screen extends StatelessWidget {
  const signup_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('signup_screen'),
      ),
      body: const Center(
        child: Text('Sign Up the form!'),
      ),
    );
  }
}
